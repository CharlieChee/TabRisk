# Law School 入学/通过（公平性常用），目标: ugpagt3
# 使用方式: python -m synthgen.train data=standard/law_school
_target_: synthgen.data.load.load_dataset
params:
  source: openml
  name: LawSchool
  # openml_id 可在 openml.org 搜索 Law school 确认后填写，不填则按 name 拉取
  openml_id: null
  split: null
  target: ugpagt3
  cache_dir: data/cache
  dropna: true
