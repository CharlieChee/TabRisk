# Pima Indians Diabetes（OpenML），医学/诊断常用
# 使用方式: python -m synthgen.train data=standard/diabetes
_target_: synthgen.data.load.load_dataset
params:
  source: openml
  name: diabetes
  openml_id: 37
  split: null
  target: Outcome
  cache_dir: data/cache
  dropna: true
