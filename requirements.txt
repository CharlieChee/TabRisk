# TabRisk 依赖（与 pyproject.toml 保持一致，用于 pip -r 或文档）
# 版本约束目的：避免 opacus 使用 torch.nn.RMSNorm 导致的 import 崩溃（见 README Dependency Compatibility Notes）

# 核心：SynthCity 后端
synthcity>=0.2.0,<0.3.0

# 显式约束：避免 nn.RMSNorm 问题（opacus 1.5+ 使用 RMSNorm，需 torch 2.10+；此处固定为 2.0 + opacus 1.x）
torch>=2.0,<2.1
opacus>=1.3,<1.5

# 其余依赖（版本与 pyproject.toml 一致）
pandas>=2.0.0
numpy>=1.24.0
pyarrow>=12.0.0
pydantic>=2.0.0
rich>=13.0.0
loguru>=0.7.0
hydra-core>=1.3.0
pytest>=7.4.0
