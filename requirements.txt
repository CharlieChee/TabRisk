# TabRisk 依赖（与 pyproject.toml 一致；权威版本以 environment.lock.yml + requirements.lock.txt 为准）
# 版本上界与 lock 一致，不超出服务器真实运行环境

# 核心：SynthCity 后端
synthcity>=0.2.0,<0.3.0

# 与 lock 一致：避免 nn.RMSNorm 问题（opacus 1.5+ 需 torch 2.10+）
torch==2.0.1
opacus==1.4.1

# 其余依赖（范围不超过 requirements.lock.txt / environment.lock.yml）
pandas>=2.0.0,<=2.3.3
numpy>=1.24.0,<=1.26.4
pyarrow>=12.0.0,<=22.0.0
pydantic>=1.10,<2
rich>=13.0.0,<=14.3.2
loguru>=0.7.0,<=0.7.3
hydra-core>=1.3.0,<=1.3.2
pytest>=7.4.0,<=9.0.2
scipy>=1.11.0,<=1.11.4
